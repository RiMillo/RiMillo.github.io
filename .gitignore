# From https://github.com/github/gitignore/blob/main/Jekyll.gitignore
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/
# Personal additions
# Notes:
# * Gemfile.lock should not be included
# * Gemfile is used by bundle to install the necessary plugin and hence is needed locally,
#   however, it is not read by GitHub Pages, so it is OK to ignore it
Gemfile*
